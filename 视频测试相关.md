### 主要工作

- [ ] 1.[Downloaded] Breakfast
- [ ] 2.[Downloaded] ActivityNet-Captions
- [ ] 3.[Downloaded] QVHighlights
- [ ] 4.[Downloaded] Charades-STA
- [ ] 5.[YouTube-Postponed] VALOR-32K

- Breakfast
- Charades-STA
- VALOR-32K

- ActivityNet-Captions
- QVHighlights

这几个视频数据集的测试

相关代码和模型我等下传到fzy@10.12.86.122:～/EAGLE文件夹下面，测试模型路径：./checkpoints/video_finetune_1epoch。

主要关注各个数据集输入的prompt的构建以及生成结果的算分，模型生成的代码我写了一个样例，./scripts/custom/eval_pointllm.sh，可以参考。模型生成的代码方面出问题的话可以找我来搞。



### 测试相关

样例代码中数据的输入格式为视频路径与问答标注，prompt的对话格式已设置好，只需传入问题。有注释的部分表示了一些可能需要修改的地方。数据集类的加载方式仅供参考，可以重写。

这些数据集都是钱隆学长做过测试的，有一些问题的话可以联系他，下面是我之前联系他得到的一些信息，可以参考：

定位任务的话就是尝试让模型输出一个起止时间戳

可以用类似这样的prompt：

```json
{'User': f'The video\'s duration is {act_video_durations[video_name[2:]]:.2f}s. Please predict the start and end times of the event: "{query}" in this video.',
'Assistant': f'The video\'s duration is {act_video_durations[video_name[2:]]:.2f}s. The event "{query}" starts at 00:'}
```

breakfast这种动作分割的可能要复杂一点,动作分割这种类型的任务的话就是尝试引诱模型输出一连串带起止时间戳的caption

可以用类似这样的prompt：

```json
{'User': f'The video lasts {duration:.1f} seconds. Please output the step-by-step actions the person is doing with start and end timestamps in the video.',
'Assistant': 'Based on the provided video, the step-by-step actions the person is doing with start and end timestamps in the video are:\nFrom 00:'}
```

算分：

算每个query的检索结果的iou，然后计算mIoU、R@{0.3,0.5,0.7}这些指标

### 数据集获取

VALOR-32K这个数据集需求YouTube爬虫，优先级较低，可以先不做

QVHighlights的视频数据源下载链接失效，我从钱隆学长那拷了一份，等下传到./dataset/QVHighlight/video下面

ActivityNet-Captions的视频数据我有一份ActivityNet- QA的，也会传到./dataset/ActivityNetCaps/video下面，不一定适配，如不需要的话随意删除

其余数据集网上有可下载来源，包括上述数据集的标注文件，也可联系钱隆学长获取，他那里可能有备份。

部分数据集的测试集部分无标注，可以使用验证集。

部分数据集可能涉及到音频部分，这部分可以忽略，只保留视觉部分。


### 个人经验

视频数据容易在测试过程中损坏，完好测过一次的视频数据也可能在下一次测试时出现损坏，可以跳过损坏的视频数据。

当前的测试代码是默认batch_size为1的，batch_size大于1时会有一定的性能损失，（当然预计性能本来也不咋样，因为训练时没在定位任务上训练😂）

之前在其他数据集上有过测试中途卡死的情况，原因未知。如有类似情况可以先咨询一下钱隆学长的经验，也可以尝试定位一下卡死的数据以及代码位置，然后联系我来搞。

其它未涉及到的问题可以再找我问一下。
